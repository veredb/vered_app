== README

This is a Rails 4 app. It contains pages, styled with css, a chat room service, blog, Newsletter service, which sends email, with attachement, on registration, Javascrip items.
The app is supported by TDD, Rspec. It containes a MySQL database.
